---

apiVersion: v1
kind: Service
metadata:
  name: httpd-service
  labels:
    type: webserver
spec:
  type: NodePort
  ports:
    - targetPort: 80
      port: 80
      nodePort: 31234
  selector:
    type: webserver
