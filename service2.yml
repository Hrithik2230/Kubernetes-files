---

apiVersion: v1
kind: Service
metadata:
  name: tomcat-service
  labels:
    author: Hrithik
spec:
  type: LoadBalancer
  ports:
    - targetPort: 8080
      port: 8080
      nodePort: 31000
  selector:
    type: appserver
    author: Hrithik
...
